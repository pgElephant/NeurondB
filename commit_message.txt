Add generic dataset generator script for test data preparation

Implement dataset.py script that accepts any command via --command flag
or positional argument, executes it in tmp directory, and generates
schema.sql and data.sql files from CSV data found in the repository.
The script analyzes CSV files to determine schema structure and
generates appropriate INSERT statements. Remove PMDB-specific
references to make the script generic and reusable for any dataset
source. Support git clone commands with automatic destination path
detection.

